GEOPULSE – Snowflake SQL & Access Documentation
Database setup, access configuration, tables, validation queries, and access testing

1.	Snowflake SQL Script
-- Database
CREATE DATABASE IF NOT EXISTS GEOPULSE;

-- Schema
USE DATABASE GEOPULSE;

CREATE SCHEMA IF NOT EXISTS RETAIL_ANALYSIS;

-- Warehouse
CREATE WAREHOUSE IF NOT EXISTS GEOPULSE_WH WAREHOUSE_SIZE = 'X-SMALL'
AUTO_SUSPEND = 60 AUTO_RESUME = TRUE;

-- Role
CREATE ROLE IF NOT EXISTS GEOPULSE_ROLE;

-- Grants
GRANT USAGE ON DATABASE GEOPULSE TO ROLE GEOPULSE_ROLE;

GRANT USAGE ON SCHEMA GEOPULSE.RETAIL_ANALYSIS TO ROLE GEOPULSE_ROLE;

GRANT USAGE ON WAREHOUSE GEOPULSE_WH TO ROLE GEOPULSE_ROLE;

GRANT SELECT ON ALL TABLES IN SCHEMA GEOPULSE.RETAIL_ANALYSIS TO ROLE GEOPULSE_ROLE;

SELECT CURRENT_USER();

SELECT CURRENT_ROLE();

SELECT CURRENT_WAREHOUSE();

CREATE DATABASE IF NOT EXISTS GEOPULSE; SHOW DATABASES;
USE DATABASE GEOPULSE;

CREATE SCHEMA IF NOT EXISTS RETAIL_ANALYSIS; USE SCHEMA RETAIL_ANALYSIS;
SHOW WAREHOUSES;

CREATE WAREHOUSE IF NOT EXISTS GEOPULSE_WH WAREHOUSE_SIZE = 'X-SMALL'
AUTO_SUSPEND = 60 AUTO_RESUME = TRUE;

USE WAREHOUSE GEOPULSE_WH;

SELECT CURRENT_WAREHOUSE();

CREATE OR REPLACE TABLE GPS_DATA ( LAT FLOAT,
LON FLOAT,
TIMESTAMP TIMESTAMP_NTZ, ACTIVITY VARCHAR, DEVICE_ID VARCHAR, PERSONA VARCHAR, DEVICE_OS VARCHAR, GPS_ACCURACY_M FLOAT, PING_ID VARCHAR, SEQUENCE_NO INTEGER, DATE DATE,
DAY_OF_WEEK VARCHAR, DISTANCE_FROM_HOME_M FLOAT
);

CREATE OR REPLACE TABLE FOOTFALL_SUMMARY (
 
STORE_ID VARCHAR, STORE_NAME VARCHAR, CATEGORY VARCHAR, DATE DATE, TOTAL_VISITS INTEGER,
TOTAL_DWELL_MINUTES FLOAT, UNIQUE_VISITORS INTEGER, AVG_DWELL_MINUTES FLOAT
);

CREATE OR REPLACE TABLE DISTANCE_DATA ( DEVICE_ID VARCHAR,
PERSONA VARCHAR, HOME_LAT FLOAT, HOME_LON FLOAT, DIST_TO_STR001_M FLOAT, DIST_TO_STR002_M FLOAT, DIST_TO_STR003_M FLOAT,
NEAREST_STORE_ID VARCHAR, NEAREST_STORE_NAME VARCHAR, DISTANCE_TO_NEAREST_STORE_M FLOAT
);

SHOW ROLES;
CREATE ROLE IF NOT EXISTS GEOPULSE_ROLE; GRANT USAGE ON DATABASE GEOPULSE
TO ROLE GEOPULSE_ROLE;

GRANT USAGE ON SCHEMA GEOPULSE.RETAIL_ANALYSIS TO ROLE GEOPULSE_ROLE;

GRANT SELECT ON ALL TABLES IN SCHEMA GEOPULSE.RETAIL_ANALYSIS TO ROLE GEOPULSE_ROLE;

GRANT CREATE TABLE ON SCHEMA GEOPULSE.RETAIL_ANALYSIS TO ROLE GEOPULSE_ROLE;

GRANT USAGE ON WAREHOUSE GEOPULSE_WH TO ROLE GEOPULSE_ROLE;

SHOW GRANTS TO ROLE GEOPULSE_ROLE; USE WAREHOUSE GEOPULSE_WH;
USE DATABASE GEOPULSE;

USE SCHEMA RETAIL_ANALYSIS;

SELECT CURRENT_ROLE();

SELECT CURRENT_WAREHOUSE();

SELECT * FROM GPS_DATA LIMIT 10; SELECT * FROM FOOTFALL_SUMMARY; SELECT * FROM DISTANCE_DATA LIMIT 10;
SELECT COUNT(*) AS GPS_ROWS FROM GPS_DATA;

SELECT COUNT(*) AS FOOTFALL_ROWS FROM FOOTFALL_SUMMARY;

SELECT COUNT(*) AS DISTANCE_ROWS FROM DISTANCE_DATA;
 
2.	GEOPULSE Snowflake Account & Access

Item	Details
Database	GEOPULSE
Schema	RETAIL_ANALYSIS
Warehouse	GEOPULSE_WH
Project Role	GEOPULSE_ROLE

Tables

No.	Table Name
1	GPS_DATA
2	FOOTFALL_SUMMARY
3	DISTANCE_DATA

Row Counts

Table	Rows
GPS_DATA	116951
FOOTFALL_SUMMARY	3
DISTANCE_DATA	500

Access Testing

Access Check	Status
Database access	PASS
Schema access	PASS
Warehouse access	PASS
GPS_DATA access	PASS
FOOTFALL_SUMMARY access	PASS
DISTANCE_DATA access	PASS

